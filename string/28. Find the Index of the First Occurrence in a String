class Solution {
public:
    int strStr(string seq, string pat) {
        int i=seq.find(pat);
        if(i!=string::npos){
            return i;
        }
        else{
            return -1;
        }
    }
};
